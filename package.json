{
  "name": "dynamic",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && rm -rf assets/ && cp -r dist/* . && git add index.html assets/ favicon.svg && echo 'Built files staged. Review with git diff --cached, then commit and push. After pushing, run: git checkout -- index.html'"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.10"
  }
}
